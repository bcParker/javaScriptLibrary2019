var isLoggedIn = true;
var isAuthenticated = true;
var hasLoggedInToday = false;
var hasToken = false;

console.log(2 > 1); //true
console.log(3 < 2); //false

var test = 2 >= 3; //What will this print? (false)
console.log(test);
console.log("Two is greater than one? " + (2 > 1));
console.log(3 >= 1);

"Password12!" === "Password12!!"  //=> false
1 !== 2  //=> true
10 !== 10  //=> false

console.log("&& :", 2===2 && 1===1 ); //true because? The 2's are the same value and type as well as the 1's
console.log("|| :", 2===2 || 2===1 ); //true because? The 2's are the same value/type but 2 and 1 are not but becuase the operator is an OR statement, one condition is true so the whole statement is true.
